language: elixir
elixir:
  - 1.6.2

addons:
  postgresql: '9.6'
  apt:
    packages:
      - postgresql-9.6-postgis-2.3

install:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get

script:
  - mix test

notifications:
  email: false
